"""Feature engineering, the transformer encoder, training, and inference.

Depends only on `schemas` (see ARCHITECTURE.md's module dependency
direction) — nothing here talks to SUMO or the scheduler directly.
"""
